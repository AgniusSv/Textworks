{% extends 'base.html.twig' %}

{% block body %}
    <h1>Savoka</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ savoka.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ savoka.type }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ savoka.description }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('savoka_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('savoka_edit', { 'id': savoka.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
